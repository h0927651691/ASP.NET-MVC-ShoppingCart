@model List<ShoppingCarts.Models.Product>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>商品列表</h2>

    @Html.ActionLink("建立", "Create", "Product", new { @class = "btn btn-default" })


@{
    @Html.Label("", (string)ViewBag.ResultMessage, new { @class = "text-info" })
 }
    
<table class="table">
    <thead>
        <tr>
            <td colspan="2">操作</td>
            <td>編號</td>
            <td>名稱</td>
            <td>描述</td>
            <td>售價</td>
            <td>庫存</td>
        </tr>
    </thead>
    <tbody >
        @foreach (var product in this.Model) /*使用foreach將所有商品列出*/
        {
        <tr class="fadeInDown animated">
            <td>
                @Html.ActionLink(
                "編輯","Edit", new { id = product.Id}, new { @class = "btn btn-default"}
                )
            </td>
            <td>
                @using (@Html.BeginForm("Delete", "Product", FormMethod.Post, new { @class = "Delete" }))
                {
                    @Html.Hidden("id", product.Id)
                    <input type="submit" value="刪除" class="btn btn-danger">
                }
            </td>

            <td>@product.Id</td>
            <td>@product.Name</td>
            <td>@product.Description</td>
            <td>@product.Price</td>
            <td>@product.Quantity</td>

        </tr>
        }
    </tbody>
</table>